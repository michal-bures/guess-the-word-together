{
    "name": "shared",
    "version": "1.0.0",
    "description": "Shared types and contants/functions for the frontend and backend",
    "types": "src/index.ts",
    "scripts": {
        "lint": "eslint . --fix",
        "lint:check": "eslint .",
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "import": "./src/index.ts"
        }
    }
}
